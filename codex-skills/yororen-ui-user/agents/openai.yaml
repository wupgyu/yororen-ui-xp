interface:
  display_name: "Yororen UI (End-User)"
  short_description: "Build gpui apps with Yororen UI"
  default_prompt: "Use $yororen-ui-user to build a gpui app with Yororen UI — routes to the right sub-skill, enforces the 3-layer split, one-call bootstrap, and the headless state pattern."
